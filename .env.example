# Server-only Neon connection string. Never prefix this with NEXT_PUBLIC_.
DATABASE_URL=postgresql://user:password@ep-example.neon.tech/dbname?sslmode=require

# Optional override; defaults to 1800 seconds.
AUTH_SESSION_TTL_SECONDS=1800
